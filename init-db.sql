-- Creates the second database for frontend user auth
-- PostgreSQL runs with default DB = publicai_foundry (backend)
-- This script creates the publicai_users DB for the frontend Prisma schema
CREATE DATABASE publicai_users;
